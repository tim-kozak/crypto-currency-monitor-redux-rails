import defaults from '../../mocks/portfolio'

export const portfolioReducer = (state = defaults, action) => {
    switch (action.type) {
        case "":

        default:
            return state;
    }
};

